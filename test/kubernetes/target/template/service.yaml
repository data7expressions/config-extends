clusterIP:
  apiVersion: v1
  kind: Service
  metadata:
    name: polaris
    namespace: polaris
    labels:
      app: polaris
  spec:
    ports:
      - name: http
        port: 8080
        protocol: TCP
        targetPort: 8080
    selector:
      app: polaris
    type: ClusterIP
