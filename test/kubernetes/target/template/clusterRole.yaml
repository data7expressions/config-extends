apiVersion: rbac.authorization.k8s.io/v1beta1
kind: ClusterRole
metadata:
  name: polaris
  labels:
    app: polaris
rules:
  - apiGroups: '${apiGroups}'
    resources: '${resources}'
    verbs: '${verbs}'
