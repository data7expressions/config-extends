clusterIP:
  apiVersion: v1
  kind: Service
  metadata:
    name: ${name}
    namespace: ${namespace}
    labels:
      app: ${name}
  spec:
    ports:
    - name: http
      port: ${port:8080}
      protocol: TCP
      targetPort: ${port:8080}
    selector:
      app: ${name}
    type: ClusterIP